import { trigger, state, style, transition, animate, keyframes, group } from '@angular/animations';

import { ExtensionMethodService } from '../../shared/extension-method.service';

// export const openCatalog = trigger('openCatalog', [
//   transition(':enter', [
//     style({

//     }),
//     animate(800, style({

//     }))
//   ])
// ]);